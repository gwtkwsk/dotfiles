#!/bin/sh

# dot_config/yazi/package.toml hash: {{ include "dot_config/yazi/package.toml" | sha256sum }}

set -eu

if ! command -v ya >/dev/null 2>&1; then
  echo "Skipping Yazi package install: 'ya' is not available" >&2
  exit 0
fi

ya pkg install
