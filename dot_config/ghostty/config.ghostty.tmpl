{{- $preset := index .themePresets .theme -}}
{{- if not $preset -}}
{{ fail (printf "Unsupported .theme value: %q" .theme) }}
{{- end -}}
window-width = 200
window-height = 50
background-opacity = 0.98
font-family = "JetBrainsMono Nerd Font"
font-size = 10
theme = {{ $preset.ghosttyTheme }}
custom-shader = shaders/cursor_warp.glsl
